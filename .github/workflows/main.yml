on:
  push:
    branches: master
jobs:
    deploy_job:
     runs-on: ubuntu-latest
     name: deploy
     steps:
      - name: Checkout
        uses: actions/checkout@v2
      

      - name: Deploy file
        uses: SamKirkland/FTP-Deploy-Action@4.3.2
        with:
         username: 'issyway@issyway.com'
         server: 'ftp://issyway.com'
         
         server-dir: '/badmin/'
         password: 'Ishanji007@@'
